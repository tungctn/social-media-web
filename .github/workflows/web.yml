name: Deploy to Servers

on:
  push:
    paths:
      - ".github/**"
    branches:
      - cicd_ssh

jobs:
  # build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v3
  #     - name: Set up Node 18.x
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: 18.x
  #     - name: Set up Ruby 3.0.2
  #       uses: ruby/setup-ruby@v1
  #       with:
  #         ruby-version: 3.0.2
  #     - name: Install dependencies frontend
  #       run: |
  #         cd ./web
  #         npm install
  #         npm run build
  #     - name: Install dependencies backend
  #       run: |
  #         cd ./api
  #         bundle install
  #         touch config/application.yml
  #         echo "${{ secrets.PROD_BACKEND }}" > config/application.yml

  deploy:
    runs-on: ubuntu-latest
    # needs: build
    strategy:
      fail-fast: false
      matrix:
        servers: ["27.66.108.160"]
    steps:
      - name: Install sshpass
        run: sudo apt-get install -y sshpass
      - name: Create SSH key
        run: |
          mkdir ~/.ssh/
          touch ~/.ssh/tungaws.pem
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/tungaws.pem
          chmod 400 ~/.ssh/tungaws.pem
      - name: Deploy to server
        run: |
          sshpass -p ${{ secrets.SUDO_PASSWORD }} ssh -o StrictHostKeyChecking=no -i ~/.ssh/tungaws.pem runner@${{ matrix.servers }} '
            node -v;
            ruby -v;  
            ls -a;
            cd social-media-web && git pull origin main && gem install bundler && cd api && bundler install;
            cd web && npm install && npm run build;
          '
